using Xunit;

namespace EtGate.UI.ViewModel.Tests
{
    // TODO: these tests will test the various composition roots are being resolved correctly. 
    // it will also try the same for all the viewmodels
    public class CanResolve
    {
        [Fact]
        void MainWindowViewModel()
        {
        }

        [Fact]
        void FlapMaintenanceViewModel()
        {
        }
    }
}
