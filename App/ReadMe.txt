It gives main app developer a hint for the scenarios:
- Qr Controller is actually a Gui Simulator, of course running in its own process
- Qr Controller is an un-managed code running in its own process
- Qr Controller is developed on an entirely different platform like native C++/Java and hence runs in its own process